// This is a placeholder file.
// In a real application, this folder would contain TypeScript type definitions
// specific to the backend, e.g., interfaces for API request bodies or responses.

/*
export interface Event {
  id: number;
  name: string;
  date: string;
  location: string;
  description?: string;
}

export interface ContactMessage {
  name: string;
  email: string;
  message: string;
}
*/
